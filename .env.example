VITE_BRAIN_API=http://127.0.0.1:8000
# Talk Mode POSTs /chat on the Brain. LLM key lives on Railway jarvis-brain, not here:
# ANTHROPIC_API_KEY=
VITE_WORKSPACE=lindsey
# Dashboard module poll interval in ms (default 30000). Watch list / money moves use 15s.
VITE_BRAIN_POLL_MS=30000
