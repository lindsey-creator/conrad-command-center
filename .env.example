# HUD (Next.js) — never commit real secrets
WHOOP_CLIENT_ID=
WHOOP_CLIENT_SECRET=
WHOOP_REDIRECT_URI=http://localhost:3000/api/whoop/callback

# Legacy Brain deck (`npm run dev:brain`) — optional
VITE_BRAIN_API=http://127.0.0.1:8000
VITE_WORKSPACE=lindsey
VITE_BRAIN_POLL_MS=30000
